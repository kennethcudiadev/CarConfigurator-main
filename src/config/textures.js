/**
 * PBR texture paths (place files in public/textures/).
 * Albedo = base color, Roughness = surface roughness, Metalness = metallic areas,
 * Normal = surface detail, AO = ambient occlusion (shadows in crevices).
 */
export const PBR_TEXTURE_PATHS = {
  map: '/textures/albedo.png',
  roughnessMap: '/textures/roughness.png',
  metalnessMap: '/textures/metalness.png',
  normalMap: '/textures/normal.png',
  aoMap: '/textures/ao.png',
}
